
export interface LatLng {
  latitude: number;
  longitude: number;
}

export interface BarberShop {
  title: string;
  uri: string;
}

export interface GroundingChunk {
  maps: {
    title: string;
    uri: string;
  };
}